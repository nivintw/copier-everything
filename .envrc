# SPDX-FileCopyrightText: © 2026 Tyler Nivin
# SPDX-License-Identifier: MIT

# This is the TEMPLATE repo's own direnv config, mirroring the pattern
# template/.envrc.jinja ships to generated projects.
if [ -d ".venv" ]; then
  export VIRTUAL_ENV="$PWD/.venv"
  PATH_add .venv/bin
fi
