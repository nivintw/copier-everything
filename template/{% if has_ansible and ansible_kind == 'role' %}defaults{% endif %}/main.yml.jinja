# SPDX-FileCopyrightText: © {{ year }} {{ author_name }}
# SPDX-License-Identifier: {{ license }}

---
# Role defaults; variables are prefixed with the role name (ansible-lint var-naming[no-role-prefix]).
# Override them in inventory or playbook vars.
{{ ansible_name }}_enabled: true
