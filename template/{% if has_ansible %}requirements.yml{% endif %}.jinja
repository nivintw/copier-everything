# SPDX-FileCopyrightText: © {{ year }} {{ author_name }}
# SPDX-License-Identifier: {{ license }}

---
# Galaxy content this project depends on, installed by `ansible-galaxy install -r requirements.yml`
# (into Ansible's default path, where ansible-lint and Molecule resolve it). `ansible.builtin.*`
# is always available and needs no entry here — add the collections and roles you import as you
# grow. For example:
#
#   collections:
#     - name: community.general
#       version: ">=8.0.0"
#   roles:
#     - name: geerlingguy.docker
collections: []
roles: []
