# SPDX-FileCopyrightText: © {{ year }} {{ author_name }}
# SPDX-License-Identifier: {{ license }}

---
# Default variables for the example role. Role variables are prefixed with the role name
# (ansible-lint var-naming[no-role-prefix]); override them in inventory or playbook vars.
example_enabled: true
